# Issues
- change progress chart y axis to have the difficulty grades instead of percentages
- split index file into sub scripts so that the project becomes more readable and maintainable
- evenly space out the header tabs inside their div to better handle white space on smaller screens

# Major feature
- rework goals
    - the intention for goals is to set it once in the spreadsheet directly, not in the web app
    - then use that set goal to determine some things like the color coding of the pyramid and the THIS WEEK panel under the Plan tab
    - The goals tab is not likely useful anymore and should be removed
    - The goals data is still stored in a sheet called goals
